/**
 * Entry point for the pet collection game application.
 */
public class Main {
    /**
     * Creates a Game instance and starts the game.
     *
     * @param args command-line arguments (not used)
     */
    public static void main(String[] args) {
        Game game = new Game();
        game.start();
    }
}
